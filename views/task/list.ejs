<% layout('layouts/boilerplate') %>
<!-- Tasks -->
<div class="bloc l-bloc py-5 bg-light" id="bloc-tasks">
    <div class="container bloc-lg bloc-no-padding-lg">
        <!-- User Info Section -->
        <div class="d-flex justify-content-between align-items-center flex-wrap mb-5">
            <div class="order-1 order-md-1 me-4 mb-3 mb-md-0">
                <img src="<%= user.avatar %>"
                     class="img-fluid rounded-circle shadow lazyload"
                     alt="User avatar"
                     width="180" height="180"
                     style="object-fit:cover;">
            </div>
            <div class="order-2 order-md-2 ms-md-5 text-center flex-grow-1">
                <h2 class="fw-bold mb-2"><%= user.fullname %></h2>
                <p class="text-muted mb-1"><i class="fa fa-user me-1"></i>Username: <span
                            class="fw-semibold"><%= user.username %></span></p>
                <p class="text-muted mb-3"><i class="fa fa-calendar me-1"></i>Member Since: <span
                            class="fw-semibold"><%= user.memberSince %></span></p>
                <a href="/tasks/new" class="btn btn-primary btn-lg px-4 mt-2" aria-label="Add Task" title="Add Task">
                    <i class="fa fa-plus me-2"></i>Add Task
                </a>
            </div>
        </div>
        <hr class="my-5">

        <!-- Task Cards -->
        <div class="row justify-content-center">
            <div class="col-12 col-lg-8">
                <div class="d-flex flex-column gap-4">
                    <% if (tasks && tasks.length) { %>
                        <% tasks.forEach(task => { %>
                            <div class="card shadow-lg border-0 rounded-4 overflow-hidden glass-card border-start border-5 
                                <%= task.status === 'completed' ? 'border-success' :
                                    task.status === 'failed' ? 'border-danger' :
                                            task.status === 'in-progress' ? 'border-info' : 'border-warning' %>">
                                <div class="card-body p-4">
                                    <div class="d-flex align-items-center mb-3">
                                        <span class="fa <%= task.status === 'completed' ? 'fa-check-circle text-success' :
                                                task.status === 'failed' ? 'fa-times-circle text-danger' :
                                                        task.status === 'in-progress' ? 'fa-spinner text-info' : 'fa-tasks text-primary' %> fa-2x me-3"></span>
                                        <div>
                                            <h4 class="card-title mb-1 fw-bold"><%= task.title %></h4>
                                            <small class="text-muted"><i
                                                        class="fa fa-calendar-alt me-1"></i>Due: <%= task.dueDate ? task.dueDate.toISOString().split('T')[0] : 'No Due Date' %>
                                            </small>
                                        </div>
                                        <span class="badge <%= task.priority === 'High' ? 'bg-danger' :
                                                task.priority === 'Medium' ? 'bg-warning text-dark' : 'bg-success' %> ms-auto align-self-start px-3 py-2 fs-6">
                                            <i class="fa <%= task.priority === 'High' ? 'fa-bolt' :
                                                    task.priority === 'Medium' ? 'fa-arrow-up' : 'fa-arrow-down' %> me-1"></i>
                                            <%= task.priority %>
                                        </span>
                                    </div>
                                    <p class="card-text text-muted mb-3">
                                        <%= task.description || 'No description provided.' %>
                                    </p>
                                    <div class="mb-3">
                                        <% (task.tags || []).forEach(tag => { %>
                                            <span class="badge bg-secondary me-1"><%= tag %></span>
                                        <% }) %>
                                    </div>
                                    <div class="d-flex justify-content-end gap-2">
                                        <a href="/tasks/<%= task._id %>/edit"
                                           class="btn btn-outline-primary btn-sm rounded-pill px-3">
                                            <i class="fa fa-edit me-1"></i>Edit
                                        </a>
                                        <form action="/tasks/<%= task._id %>?_method=DELETE" method="POST"
                                              class="d-inline">
                                            <button type="submit"
                                                    class="btn btn-outline-danger btn-sm rounded-pill px-3">
                                                <i class="fa fa-trash me-1"></i>Delete
                                            </button>
                                        </form>
                                        <% if (task.status !== 'completed') { %>
                                            <form action="/tasks/<%= task._id %>/complete" method="POST"
                                                  class="d-inline">
                                                <button type="submit" class="btn btn-success btn-sm rounded-pill px-3">
                                                    <i class="fa fa-check me-1"></i>Done
                                                </button>
                                            </form>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <p class="text-center text-muted">No tasks available. Create a new task to get started!</p>
                    <% } %>
                </div>
                <!-- Pagination -->
                <nav class="d-flex justify-content-center mt-5" aria-label="Task pagination">
                    <ul class="pagination pagination-lg">
                        <% if (pagination) { %>
                            <% for (let i = 1; i <= pagination.totalPages; i++) { %>
                                <li class="page-item <%= i === pagination.currentPage ? 'active' : '' %>">
                                    <a class="page-link" href="?page=<%= i %>"><%= i %></a>
                                </li>
                            <% } %>
                        <% } %>
                    </ul>
                </nav>
            </div>
        </div>
    </div>
</div>
<!-- Tasks End -->